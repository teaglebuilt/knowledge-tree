

## Knowledge Bank Library

A python library that reads from the tree on a compilation loop to compile and reconcile as the tree is edited.

`branches.yaml` defines the tree structure but also defines how to build the tree. From file encryption, to pulling from remote sources such as books or pdf documents.

```yaml

```